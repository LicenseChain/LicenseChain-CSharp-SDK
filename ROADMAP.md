# LicenseChain CSharp SDK Roadmap

## Current Focus

- Keep API base URL behavior aligned with `https://api.licensechain.app/v1`.
- Maintain `VerifyLicenseWithDetailsAsync` and JWKS verification helper compatibility.
- Keep parity with enterprise verify/JWKS contract expectations.

## Next Operational Tasks

- Re-run conformance checks after verify/JWKS contract changes.
- Keep docs/examples synchronized with ecosystem docs.
- Capture release-impacting changes in `CHANGELOG.md`.
